{
  "name": "@workspace/api-spec",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "codegen": "orval --config ./orval.config.ts && node -e \"const fs=require('fs'),g='../../lib/api-zod/src/generated/api.ts';fs.writeFileSync(g,fs.readFileSync(g,'utf8').replace(/from 'zod'/g,\\\"from 'zod/v4'\\\"))\" && pnpm -w run typecheck:libs"
  },
  "devDependencies": {
    "orval": "^8.23.0"
  }
}
